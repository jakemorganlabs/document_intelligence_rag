# Smoke Test Output - Closeout Slot

Placeholder. Populate after running `scripts/smoke_prod.sh` against the deployed instance.

Run:
```bash
HMAC_SECRET="your-secret" bash scripts/smoke_prod.sh
```

Expected contents:
- Signed request -> 200 + `citations` array non-empty
- Unsigned request -> 401