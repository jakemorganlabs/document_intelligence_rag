#!/usr/bin/env bash
# scripts/hooks/pre-commit — run secret gate before every commit
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
REPO_ROOT="$(cd "$SCRIPT_DIR/../.." && pwd)"

bash "$REPO_ROOT/scripts/secret_gate.sh"
